import SchoolsTeachersStudents from '../../assets/data/SchoolsTeachersStudents';

/**
|--------------------------------------------------
| Normally we'd do an asynchronous request here, such as fetch or a SAGA setup etc.
| Since we are using a non-api set of dummy data, it is more a matter of parsing the info. 
|--------------------------------------------------
*/

const fetchSchools = () => {
  console.log('Fetch Schools');
};

export default fetchSchools;